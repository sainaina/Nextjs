const config = {
  plugins: ["@tailwindcss/postcss"],
  theme:{
    extends:{
      fontFamily:{
        lexend: ["var(--font-lexend)"],
        kantumruy:["var(--font-kantumruy)"]
      }
    }
  }
};

export default config;
